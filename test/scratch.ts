/*
import axios from 'axios';
function main() {
  for (let i = 0; i < 1000; i++) {
    console.time(`a${i}`);
    axios({
      method: 'get',
      url: 'http://localhost:8080',
    }).then(() => {
      console.timeEnd(`a${i}`);
    });
  }
}

main();

*/

